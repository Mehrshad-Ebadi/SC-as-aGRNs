#include "../Headers/Genome.hpp"

int main ()
{
    int N = 10;  //Number of nodes in the network, so if you want a network of 20 nodes, set "int N = 20;" and so on ...
    genome GM;
    GM.Start(N);
}
